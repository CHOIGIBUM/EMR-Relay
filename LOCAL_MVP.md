# EMS Relay 로컬 시연 안내

AWS 계정이나 외부 Agent API 없이 한 대의 PC에서 전체 사용자 흐름을 확인할 수 있습니다.

## 실행

1. Node.js 22 이상을 설치합니다.
2. `RUN_LOCAL_MVP.bat`을 더블클릭합니다.
3. 터미널에 표시되는 주소를 브라우저에서 엽니다.
4. 종료할 때 터미널에서 `Ctrl+C`를 누릅니다.

## 권장 시연 방법

다음 주소를 네 개의 탭으로 열어 역할 전환을 보여줍니다.

- `/?view=mobile` — 구급대원 모바일
- `/?view=control` — 이송조정 상황실
- `/?view=hospital` — 병원 수용 웹
- `/?view=report` — 구급활동 기록 검토

별도의 `/?view=workflow` 화면에서는 전체 진행 상태와 자동 기록 시각을 한눈에 볼 수 있습니다. 모든 탭은 같은 사건 상태를 공유합니다.

## 로컬 대체 구성

- 음성 처리: 준비된 한국어 PTT 발화를 구조화된 변경안으로 반환
- 병원 조회: 시연용 병원 별칭 3곳의 기관 참고정보·거리·ETA 반환
- 사건 저장: 브라우저 로컬 저장소 사용
- 역할 간 전달: BroadcastChannel과 storage 이벤트로 탭 간 동기화

실제 환자정보와 실제 병원 수용정보는 사용하지 않습니다. Agent API가 응답하지 않으면 기존 환자정보를 변경하지 않고 재시도를 안내합니다.
