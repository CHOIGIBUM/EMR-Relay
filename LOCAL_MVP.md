# EMS Relay 로컬 MVP

AWS 계정이나 외부 Agent API 없이 한 대의 PC에서 전체 흐름을 확인하는 모드입니다.

## 실행

1. Node.js 22 이상을 설치합니다.
2. `RUN_LOCAL_MVP.bat`을 더블클릭합니다.
3. 실행 창에 표시된 `http://localhost:3000` 주소를 브라우저에서 엽니다.

처음 실행할 때만 필요한 구성요소를 설치하기 위해 인터넷 연결이 필요합니다. 이후 EMS Relay 시연 흐름에서는 외부 의료·AWS API를 호출하지 않습니다.

## 역할별 화면을 동시에 보기

같은 브라우저에서 다음 주소를 각각 새 탭으로 엽니다.

- 구급대원: `http://localhost:3000/?view=mobile`
- 이송조정 상황실: `http://localhost:3000/?view=control`
- 병원: `http://localhost:3000/?view=hospital`
- 전체 기록: `http://localhost:3000/?view=workflow`

한 탭에서 버튼을 누르면 다른 탭도 같은 사건 상태로 갱신됩니다. 새로고침해도 현재 시연 상태가 복원되며, 오른쪽 위 `초기화`를 누르면 처음으로 돌아갑니다.

## 로컬에서 대신 동작하는 부분

- 음성 Agent: 정해진 급성 뇌졸중 예제 발화를 환자정보 후보로 구조화합니다.
- 병원정보 조회: 강원권 예제 병원 3곳의 거리·ETA·기관 참고정보를 반환합니다.
- 사건 저장: 브라우저에 임시 저장합니다.
- 역할 간 전달: 같은 브라우저의 여러 탭을 실시간으로 동기화합니다.

실제 음성 파일, 실제 환자정보, 119 지령시스템, 병원 EMR, 실시간 수용정보는 사용하지 않습니다.

## 추후 AWS 연결

실제 API가 준비되면 `.env.local` 파일에 아래처럼 주소만 설정하도록 구성했습니다.

```text
NEXT_PUBLIC_EMS_API_BASE=https://API-GATEWAY-주소
```

프론트 화면과 업무 흐름은 유지하고 `/agent`, `/hospitals` 응답만 실제 서비스로 교체할 수 있습니다.
