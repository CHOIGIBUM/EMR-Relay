# EMS Relay 업무 흐름

## 목표와 역할

목표는 강원 영동권 65세 이상 뇌졸중 의심 환자를 골든타임 안에 **실제로 초기 처치할 수 있다고 회신한 병원**과 빠르게 연결하는 것입니다.

- **구급대원**: 현장 평가, 환자 카드 확정, 병원 동시 요청, 최종 이송지 선택, 이송과 도착 기록
- **병원 수용 담당자**: 자신에게 온 요청 열람, 환자 카드 확인, 수용 가능 또는 수용 곤란 회신
- **AI**: 구급대원의 자유 발화를 검토 가능한 변경안으로 정리

별도의 관제센터 역할은 현재 MVP에 없습니다.

## 구급대원 흐름

```text
로그인
→ 배정 사건 목록에서 사건 선택
→ 출동 시작
→ 현장 도착
→ 환자 접촉
→ 기본 평가 입력
→ CPSS 입력
→ 활력징후·마지막 정상 확인 시각·최초 이상 발견 시각 입력
→ 환자 카드 확정
→ 근거리 병원 동시 요청
→ 수용 가능 회신 병원 목록 확인
→ 최종 이송지 선택
→ Kakao 경로 확인
→ 이송 시작
→ 병원 도착
```

각 업무 버튼의 시각은 서버 이벤트로 기록합니다. 환자 상태 측정 시각은 구급대원이 입력한 값을 보존하며 AI 처리 시각으로 덮어쓰지 않습니다.

## 현장 평가 최소 항목

- 연령, 성별
- 기도, 호흡, 순환, AVPU
- 주호소
- CPSS: 안면마비, 팔 떨어짐, 언어 이상
- 혈압(`mmHg`), 맥박(`회/분`), 호흡수(`회/분`), SpO₂(`%`), 혈당(`mg/dL`)
- 마지막 정상 확인 시각과 근거
- 최초 이상 발견 시각
- 활력징후 측정 시각

필수 항목이 확인되기 전에는 환자 카드 확정과 병원 요청을 진행할 수 없습니다.

## 수동 입력과 음성 입력

### 수동 입력

구조화된 선택·수치 입력은 AI를 거치지 않습니다. 서버가 형식과 범위를 검증한 뒤 구급대원의 확정값으로 저장합니다.

### PTT 음성 입력

1. 브라우저가 16 kHz mono PCM을 Transcribe Streaming으로 보냅니다.
2. 인식된 한국어 문장을 음성 Lambda가 받습니다.
3. 명확한 활력징후 문장은 규칙으로 빠르게 구조화합니다.
4. 자유로운 구어체 문장은 Bedrock의 Claude 모델이 변경안을 만듭니다.
5. 화면에 원문, 변경 항목, 확인 필요 표시를 보여줍니다.
6. 구급대원이 선택한 항목만 확정 입력에 반영합니다.

모든 음성 변경안은 `PENDING`이며 자동 저장되지 않습니다.

## 병원 매칭 흐름

```text
환자 카드 + 현장 좌표
→ 15 km 이내 ETA 우선 후보 최대 3곳에 동시 요청
→ 병원별 요청이 실시간 수용 화면에 도착
→ 수용 가능 회신이 없으면 45초 뒤 30 km로 확대
→ 같은 방식으로 60 km, 최대 120 km까지 확대
→ 첫 수용 가능 회신이 생기면 자동 확대 중단
→ 구급대원이 수용 가능 병원 중 최종 선택
```

NMC와 HIRA는 기관 및 진료역량 참고정보를, Kakao는 도로 거리와 ETA를 제공합니다. 이 데이터는 후보 탐색에만 쓰며 병원 수용을 추정하지 않습니다.

## 병원 흐름

```text
로그인
→ 요청 대기
→ 좌측 신규 환자 요청 선택
→ 환자 카드와 필요 처치·거리·ETA 확인
→ 수용 가능 또는 수용 곤란 회신
→ 선택된 병원은 구급대원의 이송 시작과 병원 도착 상태 확인
```

병원 계정은 Cognito `custom:hospital_id`와 일치하는 요청만 볼 수 있습니다. 수용 곤란 회신에는 필요할 때 사유를 남길 수 있습니다.

## 실시간 동기화와 저장

- AppSync Subscription은 Cognito JWT로 인증된 WebSocket을 통해 구급대원 사건과 병원 inbox의 변경을 전달합니다.
- 구급대원은 자신에게 배정된 사건별 `onCaseUpdate`, 병원은 Cognito `custom:hospital_id`의 `onHospitalInbox`만 구독합니다.
- 변경 알림은 짧게 합쳐 즉시 최신 스냅샷을 다시 조회합니다. 연결 실패·재연결 중에만 2초 폴링이 자동 대체 경로로 동작합니다.
- DynamoDB가 사건 메타, 확정 환자정보, 이벤트, 병원 요청, 매칭 작업의 단일 기준입니다.
- SQS는 거리 범위별 매칭을 UI 요청과 분리해 느린 외부 API가 화면 조작을 막지 않도록 합니다.

## 책임 경계

- 병원의 `수용 가능` 회신과 구급대원의 `최종 이송지 선택`은 별도 단계입니다.
- AI는 진단·치료·수용·이송지 선택을 대신하지 않습니다.
- 병원 기관정보, 의료자원, 거리, ETA는 참고정보입니다.
- 미확인 값은 `없음`으로 바꾸지 않고 미확인 상태로 유지합니다.
- 실제 의료 현장 적용 전에는 소방·병원 업무규정, 개인정보보호, 보안 및 임상 검증이 별도로 필요합니다.
